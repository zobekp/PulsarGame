# CLAUDE.md

Canonical project context lives in **`AGENTS.md`** and **`docs/DESIGN.md`** — read those first,
then `docs/CLASSES.md` and `docs/ROADMAP.md`. Everything in `AGENTS.md` applies here verbatim.

Quick reminders: vanilla JS + Canvas 2D, no build step; `data/config.js` is the only tuning
surface; build the current `ROADMAP.md` phase only; update `docs/DEVLOG.md` at the end of every
session.
