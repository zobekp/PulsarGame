# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Node (arrives at Phase 5 server)
node_modules/
npm-debug.log*
yarn-error.log*
.env
.env.local

# Build / dist (none through prototype phases)
dist/
build/

# Logs
*.log

# Claude Code local config (machine-specific)
.claude/

# public build artifact (rebuilt per deploy)
dist/
